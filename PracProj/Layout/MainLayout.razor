@inherits LayoutComponentBase
<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        @* todo: Auth state *@
        <div class="top-row px-4">
            <Button Color="ButtonColor.Primary"> Регистрация </Button>

            <Button Color="ButtonColor.Danger"> Выйти </Button>

            <Button Color="ButtonColor.Primary"> Войти </Button>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>
